# HANDOFF — 超数秘術ギルド 状態ドキュメント

このファイルは、セッション（モデル）が切り替わるたびに引き継ぎが失われる問題への対策として作った、
プロジェクトの外部記憶です。ここに書かれていることが現在の正で、チャット履歴を遡らなくても
この1枚を読めば状況に追いつけることを目指します。**何か決まったら、都度このファイルを更新してください。**

## これは何か

超数秘術（誕生日を一切使わず、名前の音だけで独自計算する数秘術体系）を軸にしたコミュニティ構想と、
そこから発行するギルドカードの実装。超数秘術**本体アプリ**は別スレッド・別運用でCloudflare上に公開済み
（このリポジトリの管理対象ではない）。このリポジトリ（pandora）は、本体から派生した
「ギルドカード発行ツール」を持つ。

## 目的（すべての判断はここに帰着する）

- 超数秘術の体系そのものを届けたい。バズらせる・大量消費させることは目的ではない
- 「量」ではなく「深く刺さる人」を見つけたい
- 新しいアイデアが出るたびに、以下4点で検査する：
  1. 実在の特定の人物に紐づいているか
  2. 相応のコスト（本気度）を相手に要求しているか
  3. AIが使えなくなっても残るか（API依存にしない。コピペでどのAIでも動く）
  4. 無限複製で消費される構造になっていないか

## 経緯で却下したもの（同じ提案を繰り返さないためのメモ）

- **OGPシェアなどバズ狙いの導線** → 目的（深さ）と逆行するため却下
- **「神様プロンプト」的な無限複製コンテンツ**（誰にでも当てはまる曖昧な内容で拡散する設計）→ 目的と矛盾するため却下
- **インナーウルフ**（人狼ゲーム化した対話ツール）→ ゲームの筐体が現実との距離を作り、作った本人も飽きて使わなくなった。方向として撤退済み
- **RPGシートとドッペルゲンガーを技術的に「ボタン一つで」繋ぐ案** → 却下。「拡張（広げる）」と「収束（深める）」は別の力学で、機械で繋がず、人間が間に立って繋ぐのが良いという結論

## 採用している設計思想

- **ハード×ソフト**：ハード＝名前由来の不変の構造、ソフト＝今この瞬間の感情・状態
- **読みは常に仮説**。断定しない
- **依存させない**。対話が深まるほど相手がその仕組みを必要としなくなるのが理想
- **深さは層として見せる**。均さない、大量消費させない

## 全体像：超数秘術ギルド（コミュニティ構想）

モンスターハンターの集会所のような、招待制コミュニティ。5段階の「深さの階段」として設計している。

**インフラ方針の変更（2026-07-02）**：受け皿はDiscordから**LINEオープンチャット**に変更。
理由：参入のしやすさ（全員LINEを持っている）と、本体アプリの導線が既にLINEであること。
書庫は当面、ギルドマスターが良いログだけを手元にコピペして保存する（日付＋太陽数・キャラ名を一行添える）。
公開の書庫（md置き場等）は記録が溜まってから考える。
受付嬢のLINEボット化は検討の末**却下**（各自のAIで巻物を召喚する方式を維持。API依存にしない思想と、
召喚のコピペ自体が「相応のコスト」になるため）。

| 段階 | 内容 | 現状 |
|---|---|---|
| 0. 出会い | 自分の言葉で書いた実例（生命の樹の紐解き等） | 既存だが手応えは弱い |
| 1. 拡張 | ギルドカード発行。誰でも軽く、名前だけで作れる | **実装済み**（`guildcard.html`） |
| 2. 応答の扉 | カード発行→受付嬢がそのカード固有の「問い」を渡す→本人がカードと問いを持って集会所に初投稿する | **設計は完了**。受付嬢の【入口の儀式】として実装済み。あとはLINEオプチャという受け皿（インフラ）を作るだけ |
| 3. 収束 | ドッペルゲンガー（AIに分身として召喚し、一人と深く対話する） | 別スレッドで実装済み・稼働中 |
| 4. 天井 | セッション（本人＝ギルドマスターと人間として会う） | 本体アプリの導線として既存 |

## リポジトリの現状（このrepo = pandora）

- **`guildcard.html`** — 唯一の実装ファイル。単体で完結（外部API・PWA不要、依存ゼロ）
  - 姓名（ひらがな）を入力 → 太陽数・33キャラクター・五行・9ステータス・ロール
    （アタッカー/サポート/タンク/ヒーラー/バランサー）を算出
  - デザインは**禁書スタイル固定**（和紙・朱印・筆文字）。**縦型（スマホ・共有用、デフォルト）と
    横型（印刷・PC用）の切り替え式**。情報量は両者同一で、縦型は上にキャラ画像→型→ロール→ステータスの順。
    画面がカードより狭い端末では自動で縮小表示される（PNG出力の実寸は変わらない）
  - キャラ画像：`bg/{太陽数}.{キャラ名}.png`（例：`bg/17.闘士.png`）。
    **超数秘術本体アプリと同じ命名規則・同じフォルダ**を想定しているので、本体の画像をそのまま流用できる。
    画像が無い番号は、キャラ名の縦書き透かしになる（33枚揃える必要はない）
  - **「受付嬢の巻物」v1.2** を内蔵：ギルド専用のAI受付プロンプト。記憶を持たない分身として、
    ユーザー自身のAI（ChatGPT等、何でもよい）で毎回召喚される。役割は「読み・問い・整形・案内」。
    v1.2で【読み】を追加：「自分てどんな人？」にはカードの範囲でざっくり解説してよい（必ず仮説形）。
    ただし深い悩み相談が始まったら【去り際】で一度受け止めて行き先を示し、受付記録を出して気持ちよく去る。
    人生相談・個別判断・カードの外の診断は引き受けない設計。対話の最後に【受付記録】を固定書式で出力させ、
    それを人力でDiscordの書庫チャンネルにコピペするのが「記憶」の仕組み（AI側は何も保持しない）
  - PNG保存、履歴機能（localStorage、選択して削除可）あり
  - 巻物の改版はギルドマスターのみが行う、という一文が入っている
  - 受付嬢は番号・キャラ名・数秘族を「共通語」として堂々と使ってよい設計
    （※ドッペルゲンガーは逆に出典を隠す設計。混同しないこと）
- **`bg/README.md`** — 画像フォルダの説明
- 旧`index.html`（RPGシート、ホロ/禁書テーマ切替版）は**削除済み**。
  理由：別スレッドで完成しているホログラフィック版と役割が重複するため

## 直近で直したバグ

- 画像パスを`bg/{太陽数}.{キャラ名}.png`に統一（旧`sumie/{太陽数}.png`命名を撤回）
- 背景の折り目線（横線2本）が文字と重なり読みにくかったため削除
- 履歴の「選択を削除」が機能しないバグを修正
  （プルダウンで選ぶと`gen()`が履歴一覧を再描画し、選択状態がリセットされていたのが原因）
- キャラ画像が未登録の太陽数でPNG保存すると生成が丸ごと失敗するバグを修正
  （非表示になった`<img>`の読み込み失敗でhtml-to-imageが落ちていた。画像は一枚ずつ足す設計なので本番でも踏む道だった）

## 残っている作業

1. **LINEオープンチャットの開設**（集会所。案内文に受付嬢の召喚手順を書く）— 唯一の未着手インフラ
2. 受付嬢の巻物v1.2を実際に無料AIで一周させて検証。崩れる箇所があればv1.3として締め直す
   （「書庫（Discord）」表記のプラットフォーム非依存化はv1.2で対応済み）
3. 創設メンバー（すでに本気で返答をくれた人たち）への声かけ
4. （優先度低）キャラ画像は33枚揃えるタスクにしない。実際に入会したメンバーの太陽数から、
   気が向いた時に一枚ずつ足していく（ギルドの歴史と一緒に埋まっていく設計）
